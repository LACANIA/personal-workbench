# Video2Skill Legacy Contract Adaptation

本目录只包含经过 STEP-15 审计后重新表达的纯数据契约与状态转换表。适配依据来自旧仓库的 `TaskManifest`、`ToolResult`、Artifact 合约和任务状态设计；适配版本不读取旧工程目录，也不包含下载、Cookie、FFmpeg、ASR、OCR、自动更新器、Shell 或网络行为。

旧工程没有顶层许可证文件。公开源码仓库只保留重新表达的契约适配代码，本机来源路径、原始文件哈希和验收证据不会提交。该适配代码继续遵循当前仓库的 `UNLICENSED` 状态；如需再分发，必须先完成独立许可审查。
